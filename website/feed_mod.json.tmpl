[
{{- range $i, $item := .Items}}
{{- if $i}},{{end}}
  {
    "link": "{{$item.Link}}",
    "title": {{ printf "%q" $item.Title }},
    "publication": "{{publication $item}}",
    "domain": "{{domain $item}}"
  }
{{- end}}
]
